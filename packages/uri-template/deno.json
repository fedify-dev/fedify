{
  "name": "@fedify/uri-template",
  "version": "2.3.0",
  "license": "MIT",
  "exports": {
    ".": "./src/mod.ts"
  },
  "description": "RFC 6570 URI Template expansion and round-trip pattern matching for Fedify",
  "author": {
    "name": "Chanhaeng Lee"
  },
  "imports": {},
  "exclude": [
    "dist",
    "node_modules"
  ],
  "publish": {
    "exclude": [
      "**/*.test.ts",
      "tsdown.config.ts"
    ]
  },
  "tasks": {
    "bench": "deno test --allow-env bench/",
    "check": "deno fmt --check && deno lint && deno check src/",
    "test": "deno test --allow-env src/"
  }
}
