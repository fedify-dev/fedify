/**
 * @module
 * Embedded ActivityPub debug dashboard for Fedify.
 *
 * This module provides a `createFederationDebugger()` function that wraps
 * an existing `Federation` object, adding a real-time debug dashboard
 * accessible via a configurable path prefix.
 */
export {};
